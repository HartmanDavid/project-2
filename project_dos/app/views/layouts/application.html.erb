<!DOCTYPE html>
<html>
<head>
  <title>ProjectDos</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="#">DoggYears</a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
          <form class="navbar-form navbar-right">
            <% if current_user %>
            <div class="form-group">
              <p>You are logged in as: <%= current_user.email %></p>
            </div>
            <div class="form-group">
              <%= link_to 'Logout',session_path(session), class: "btn btn-success", method: :delete, data: {confirm: "Are you sure?"}%>
            </div>
          </form>
          <%else%>
          <%= render 'sessions/login'%>
          <%end%>
        </div><!--/.navbar-collapse -->
      </div>
    </nav>

    <!-- Main jumbotron for a primary marketing message or call to action -->
    <div class="jumbotron">
      <div class="container">
        <h1>Welcome to DoggYears</h1>
        <p>
          Are you curious about whats in your pets food, would you like to keep your pet alive longer? Then sign up now!
        </p>
      </div>
    </div>


      <hr>

      <footer>
        <p>&copy; DoggYears 2015</p>
      </footer>
    </div>
    <%= yield %>

</body>
</html>
